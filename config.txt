#   Make sure you set the below fields
#   Don't commit passwords to Github :)


admin.email =
admin.password =


toastweb.url = https://localhost:8443

emails.filepath = emails.txt
users.filepath = users.json